// import { Accounts } from 'meteor/accounts-base'
// import { email, username } from './PROTECTED_VARIABLES'
//
// Accounts.onCreateUser(function(options, user) {
//   //console.log('onCreateUser is fired!')
//   if (options.username === username || options.email === email) options.profile.isAdmin = true;
//   if (options.profile) user.profile = options.profile
//   return user;
// });
